type Point = struct {
	x: int64
	y: int64
}

func main() -> int64 {
	let p: Point = Point(x: 3, y: 4)
	print_int(p.x)
	print_int(p.y)
	p.x = 10
	print_int(p.x)
	return 0
}

// expected output:
// 3
// 4
// 10

// expected status:
// 0

// expected selfhosted:
// skip
