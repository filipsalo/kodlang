// Methods on the bool primitive type. The compiler dispatches
// `b.to_str()`, `b.hash()` etc. for bool receivers to these functions.

func to_str(self: bool) -> str {
	if self {
		return "true"
	}
	return "false"
}

func hash(self: bool) -> int64 {
	if self {
		return 1
	}
	return 0
}
