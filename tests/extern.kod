// Extern functions
extern func puts(anon s: str) -> int64

func greet(anon name: str) -> none {
	puts(name)
}

func main() -> int64 {
	greet("hello from extern")
	return 0
}

// expected output:
// hello from extern

// expected status:
// 0

// expected selfhosted:
// skip
