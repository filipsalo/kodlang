{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Kod","text":"<p>Kod is a toy language that compiles to AArch64 (Apple Silicon) machine code for macOS. A Python frontend bootstraps the self-hosted compiler (<code>sh_kodc</code>), which compiles Kod programs — including itself.</p> <pre><code>func main() -&gt; int64 {\n    print(\"Hello, world!\")\n    return 0\n}\n</code></pre> <pre><code>$ uv run kod build hello.kod\n$ ./build/apps/hello/hello\nHello, world!\n</code></pre>","path":["Kod"],"tags":[]},{"location":"#features","level":2,"title":"Features","text":"<ul> <li>Compiled to native ARM64 machine code (via <code>as</code> + <code>ld</code>)</li> <li>Static types: <code>int64</code>, <code>str</code>, <code>bool</code>, arrays <code>[T]</code>, structs, enums, optionals <code>T?</code>, generics <code>T[U, V]</code></li> <li>Pattern matching via <code>match</code>, plus <code>if X is .Variant(bindings)</code> sugar</li> <li>F-string interpolation (<code>f\"hello {name}\"</code>), auto-<code>to_str</code> for non-string interpolands</li> <li>Interfaces with vtable dispatch; primitives implement them via implicit boxing</li> <li>Error handling via <code>T or Error</code>, <code>try</code> / <code>must</code> / <code>throw</code></li> <li><code>test \"description\" { ... }</code> syntax with <code>assert &lt;expr&gt;</code> (source captured)</li> <li>Modules and imports (<code>import \"kod/ast\"</code>, <code>import \"./helper\"</code>)</li> <li>Self-hosted ARM64 codegen written in Kod itself</li> </ul>","path":["Kod"],"tags":[]},{"location":"#status","level":2,"title":"Status","text":"<p>The self-hosted compiler (<code>sh_kodc</code>) is the production path. The Python frontend (<code>kod _interpret</code>, <code>kod _emit-asm</code>) remains for development and bootstrapping.</p>","path":["Kod"],"tags":[]},{"location":"arrays/","level":1,"title":"Arrays","text":"<p>Arrays in Kod are resizable, homogeneous sequences. The type is <code>[T]</code> where <code>T</code> is the element type.</p>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#literals","level":2,"title":"Literals","text":"<pre><code>let nums: [int64] = [1, 2, 3, 4, 5]\nlet words: [str] = [\"hello\", \"world\"]\nlet empty: [int64] = []\n</code></pre>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#indexing","level":2,"title":"Indexing","text":"<pre><code>let first: int64 = nums[0]\nlet last: int64 = nums[-1]    // negative index counts from end\nlet third: int64 = nums[2]\n</code></pre> <p>Negative indices wrap around: <code>arr[-1]</code> is equivalent to <code>arr[len(arr) - 1]</code>.</p>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#length","level":2,"title":"Length","text":"<pre><code>let n: int64 = len(nums)\n</code></pre>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#slicing","level":2,"title":"Slicing","text":"<pre><code>let sub: [int64] = nums[1..4]   // elements 1, 2, 3\nlet prefix: [int64] = nums[..3]  // first three\nlet suffix: [int64] = nums[3..]  // from index 3 onward\nlet copy: [int64] = nums[..]     // shallow copy\n</code></pre> <p>Both endpoints clamp to <code>[0, len]</code>; out-of-range bounds yield an empty slice.</p>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#concatenation","level":2,"title":"Concatenation","text":"<p>Use <code>+=</code> to append elements or extend with another array:</p> <pre><code>let a: [int64] = [1, 2]\na += [3, 4]          // a is now [1, 2, 3, 4]\n</code></pre> <p>Or <code>+</code> to produce a new array:</p> <pre><code>let c: [int64] = a + b\n</code></pre>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#iterating","level":2,"title":"Iterating","text":"<p>Index-based:</p> <pre><code>let i: int64 = 0\nfor i &lt; len(nums) {\n    print_int(nums[i])\n    i = i + 1\n}\n</code></pre> <p>Foreach:</p> <pre><code>for n in nums {\n    print_int(n)\n}\n</code></pre>","path":["Language guide","Arrays"],"tags":[]},{"location":"arrays/#memory-layout-compiler","level":2,"title":"Memory layout (compiler)","text":"<p>Arrays are represented as a header struct on the heap:</p> <pre><code>{ void* ptr, int64 len, int64 cap }\n</code></pre> <p>The <code>ptr</code> field points to an arena-allocated block of elements, each 8 bytes wide. <code>len</code> is the number of live elements; <code>cap</code> is the allocated capacity.</p> <p>Appending (<code>+=</code>) allocates a new backing buffer via <code>_kod_arr_concat</code> in the runtime.</p>","path":["Language guide","Arrays"],"tags":[]},{"location":"builtins/","level":1,"title":"Built-ins","text":"<p>These names live in <code>stdlib/builtins.kod</code> and are available in every Kod program without an explicit import.</p>","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#output","level":2,"title":"Output","text":"Function Signature Description <code>print</code> <code>(str) -&gt; none</code> Print a string followed by a newline. <code>print_int</code> <code>(int64) -&gt; none</code> Print an integer followed by a newline. Equivalent to <code>print(n.to_str())</code>. <code>puts</code> <code>(str) -&gt; int64</code> C <code>puts</code>. <code>putchar</code> <code>(int64) -&gt; int64</code> Write a single byte to stdout.","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#length","level":2,"title":"Length","text":"Function Signature Description <code>len</code> <code>(str) -&gt; int64</code> String length in bytes. <code>len</code> <code>([T]) -&gt; int64</code> Number of elements in an array. <code>len</code> <code>(SomeStruct) -&gt; int64</code> Dispatches to the struct's <code>len(self) -&gt; int64</code> method if one exists (e.g. <code>Map.len</code>).","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#string-conversion","level":2,"title":"String conversion","text":"<p>There is no <code>int_to_str</code> builtin — primitives carry their own <code>to_str</code> methods:</p> <pre><code>let s: str = 42.to_str()      // \"42\"\nlet t: str = true.to_str()    // \"true\"\nlet u: str = \"hi\".to_str()    // identity\n</code></pre> <p>F-strings interpolate non-<code>str</code> values by calling <code>to_str</code> automatically:</p> <pre><code>print(f\"n = {42}\")            // n = 42\n</code></pre>","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#hashing","level":2,"title":"Hashing","text":"<pre><code>let h: int64 = \"hello\".hash()\nlet g: int64 = 99.hash()\n</code></pre> <p><code>int64.hash</code> is the identity function; <code>str.hash</code> is djb2; <code>bool.hash</code> is 0/1. Used internally by <code>Map[K, V]</code>.</p>","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#mapk-v","level":2,"title":"<code>Map[K, V]</code>","text":"<p>A built-in generic. Open-addressing hash map (PEP 468 dense + sparse).</p> <pre><code>let m: Map[str, int64] = Map[str, int64]()\nm.set(\"a\", 1)\nm.set(\"b\", 2)\nmatch m.get(\"a\") {\n    Some(v) -&gt; print(v.to_str())\n    none -&gt; print(\"miss\")\n}\nprint(len(m).to_str())   // 2\n</code></pre>","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#error-handling","level":2,"title":"Error handling","text":"Name Description <code>Error</code> (interface) Marker for types that can be <code>throw</code>n. Requires <code>to_str(self) -&gt; str</code>. <code>ExitCode</code> (interface) Optional refinement: a custom <code>exit_code(self) -&gt; int64</code> controls the process exit when <code>main</code> returns an <code>Err</code>. <code>kod_panic</code> (extern) Print <code>panic: &lt;msg&gt;\\n</code> to stderr and <code>exit(1)</code>. Used by <code>must</code> and by <code>assert</code> outside a test. <code>kod_eprint</code> (extern) Write to stderr without exiting.","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#testing","level":2,"title":"Testing","text":"Name Description <code>fail(msg)</code> Mark the current <code>test \"...\" {}</code> as failed; outside a test, no-op. <code>assert &lt;expr&gt;</code> Statement form. Inside a test, flag on <code>false</code>; outside, panic. Source text is captured at parse time. See Testing.","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#io","level":2,"title":"I/O","text":"<p>The <code>io</code> module wraps the filesystem primitives. Both calls require <code>import \"io\"</code> — touching the filesystem is explicit at the import line.</p> <pre><code>import \"io\"\n\n// Read whole file. Returns \"\" if the file can't be opened.\nlet contents: str = io.read_file(\"data.txt\")\n\n// Write a file, replacing it if it exists. Returns 0 on success,\n// -1 on failure. The parent directory must already exist.\nlet rc: int64 = io.write_file(\"out.txt\", \"hello\\n\")\n</code></pre> <p>For subprocess support, see <code>stdlib/process.kod</code>:</p> <pre><code>import \"process\"\n\nlet r: process.ProcessResult = process.run([\"echo\", \"hello\"])\nassert r.status == 0\nassert r.stdout == \"hello\\n\"\n</code></pre>","path":["Reference","Built-ins"],"tags":[]},{"location":"builtins/#extern-c-runtime","level":2,"title":"Extern (C runtime)","text":"<p>Available for re-declaration in any module if needed:</p> Symbol Description <code>_strlen</code> C <code>strlen</code>. <code>_arena_alloc</code> Bump allocator for everything heap-shaped. <code>_kod_str_concat</code>, <code>_kod_str_slice</code> String operations the runtime exposes to codegen. <code>_kod_array_concat</code> Array concatenation. <code>_kod_test_fail</code>, <code>_kod_test_summary</code> Test runner internals. <code>read_file</code>, <code>write_file</code> File I/O. <p>Direct use is rarely needed — the language-level operators cover most cases.</p>","path":["Reference","Built-ins"],"tags":[]},{"location":"cli/","level":1,"title":"CLI reference","text":"<p>The CLI is <code>kod</code> (installed as a project script). Run via <code>uv run kod ...</code> from the project, or install globally with <code>uv tool install .</code>.</p>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#public-commands","level":2,"title":"Public commands","text":"","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#kod-build-file","level":3,"title":"<code>kod build &lt;file&gt;</code>","text":"<p>Compile a Kod source file to a native ARM64 binary.</p> <pre><code>uv run kod build hello.kod\n./build/apps/hello/hello\n</code></pre> <p>Build pipeline:</p> <ol> <li>Parse all transitively-imported modules.</li> <li>Emit ARM64 assembly for each module (via <code>sh_kodc</code> when available, Python interpreter as fallback during bootstrap).</li> <li>Assemble each <code>.s</code> with <code>as</code>.</li> <li>Compile <code>stdlib/{arena,runtime}.c</code> with <code>clang</code>.</li> <li>Generate the <code>_main</code> runtime shim that calls the user's <code>main</code>.</li> <li>Link everything with <code>ld</code>.</li> </ol>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#kod-run-file","level":3,"title":"<code>kod run &lt;file&gt;</code>","text":"<p><code>kod build</code> + execute. Arguments after the file are passed to the program.</p> <pre><code>uv run kod run hello.kod\n</code></pre>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#kod-test-path","level":3,"title":"<code>kod test &lt;path&gt;</code>","text":"<p>Run <code>test \"...\" { ... }</code> blocks. Path can be a file or a directory.</p> <ul> <li>File: builds the file + all transitively-imported modules that contain tests, runs everything in one binary, prints a rolled-up summary.</li> <li>Directory: walks for <code>*_test.kod</code> and runs them all together via a temporary aggregator.</li> </ul> <pre><code>uv run kod test stdlib/map_test.kod\nuv run kod test stdlib            # everything under stdlib/\nuv run kod test .                 # everything in the project\n</code></pre> <p>See Testing for the syntax.</p>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#kod-check-file","level":3,"title":"<code>kod check &lt;file&gt;</code>","text":"<p>Run the full compile pipeline through codegen, report errors, but write no artifacts and skip the link step. Exits non-zero on any error.</p> <pre><code>uv run kod check src/foo.kod\n</code></pre>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#kod-fmt-file","level":3,"title":"<code>kod fmt &lt;file&gt;</code>","text":"<p>Not yet implemented.</p>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#build-outputs","level":2,"title":"Build outputs","text":"Path Description <code>build/stage0/</code> Shared stdlib objects: <code>arena.o</code>, <code>runtime.o</code>, <code>_builtins.o</code>, <code>_int64.o</code>, <code>_str.o</code>, <code>_bool.o</code>. Reused by <code>sh_kodc</code> and every app. <code>build/stage1/</code> The self-hosted compiler <code>sh_kodc</code> and its parts (<code>lexing.{s,o}</code>, <code>parsing.{s,o}</code>, <code>codegen.{s,o}</code>, <code>kodc.{s,o}</code>, <code>runtime_main.{s,o}</code>). <code>build/apps/&lt;stem&gt;/</code> Per-app artifacts: each module's <code>.s</code> and <code>.o</code>, the runtime_main shim, and the final executable (or <code>&lt;stem&gt;_test</code> for <code>kod test</code>). <p><code>make clean-apps</code>, <code>make clean-stage1</code>, <code>make clean</code> give scoped cleanup.</p>","path":["Reference","CLI reference"],"tags":[]},{"location":"cli/#internal-debug-commands","level":2,"title":"Internal / debug commands","text":"<p>These exist to support the bootstrap and for debugging. Their shape may change.</p> Command Purpose <code>kod _interpret &lt;file&gt;</code> Run via the Python interpreter (slow; used by sh_kodc's bootstrap). <code>kod _emit-asm &lt;file&gt;</code> Print the assembly for the entry module to stdout. <code>kod _dump-ast &lt;file&gt;</code> Pretty-print the parsed AST. <code>kod _emit-runtime-main &lt;entry.kod&gt; &lt;out.s&gt;</code> Write the <code>_main</code> shim for the given entry. Delegates to sh_kodc when available.","path":["Reference","CLI reference"],"tags":[]},{"location":"control-flow/","level":1,"title":"Control flow","text":"","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#if-else","level":2,"title":"<code>if</code> / <code>else</code>","text":"<pre><code>if x &gt; 0 {\n    print(\"positive\")\n} else {\n    print(\"non-positive\")\n}\n</code></pre> <p>The <code>else</code> branch is optional. Braces are always required.</p>","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#for-while-style","level":2,"title":"<code>for</code> (while-style)","text":"<p><code>for</code> with a condition loops until the condition is false:</p> <pre><code>let i: int64 = 0\nfor i &lt; 10 {\n    print_int(i)\n    i = i + 1\n}\n</code></pre> <p>This is Kod's general looping construct. There is no <code>while</code> keyword.</p>","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#for-in-foreach","level":2,"title":"<code>for</code> / <code>in</code> (foreach)","text":"<p>Iterate over an array with <code>for … in</code>:</p> <pre><code>let names: [str] = [\"alice\", \"bob\", \"carol\"]\nfor name in names {\n    print(name)\n}\n</code></pre> <p>The loop variable is declared implicitly — no <code>let</code> needed.</p>","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#break-and-continue","level":2,"title":"<code>break</code> and <code>continue</code>","text":"<p>Works inside both loop forms:</p> <pre><code>let i: int64 = 0\nfor i &lt; 100 {\n    if i == 5 {\n        break\n    }\n    i = i + 1\n}\n</code></pre> <pre><code>for x in items {\n    if x == skip_value {\n        continue\n    }\n    process(x)\n}\n</code></pre>","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#match","level":2,"title":"<code>match</code>","text":"<p>Pattern matching on enums, integers, and strings. See Pattern matching.</p> <pre><code>match direction {\n    Direction.North -&gt; print(\"north\")\n    Direction.South -&gt; print(\"south\")\n    else -&gt; print(\"other\")\n}\n</code></pre>","path":["Language guide","Control flow"],"tags":[]},{"location":"control-flow/#return","level":2,"title":"<code>return</code>","text":"<p>Return a value from a function:</p> <pre><code>func abs(n: int64) -&gt; int64 {\n    if n &lt; 0 {\n        return 0 - n\n    }\n    return n\n}\n</code></pre> <p>A bare <code>return</code> (no value) is valid in <code>-&gt; none</code> functions.</p>","path":["Language guide","Control flow"],"tags":[]},{"location":"enums/","level":1,"title":"Enums","text":"<p>Enums in Kod are tagged unions — each value carries a discriminant identifying which variant it is, plus an optional payload with named fields.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#declaration","level":2,"title":"Declaration","text":"<pre><code>type Direction = enum {\n    North\n    South\n    East\n    West\n}\n\ntype Message = enum {\n    // unit variant (no payload)\n    Quit\n    // payload variants\n    Text(content: str)\n    Move(x: int64, y: int64)\n}\n</code></pre> <p>Comments are allowed inside the body. Variants without parentheses are unit variants; variants with parentheses carry named fields.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#construction","level":2,"title":"Construction","text":"<pre><code>let d: Direction = Direction.North\nlet m: Message = Message.Text(content: \"hello\")\nlet q: Message = Message.Quit\n</code></pre>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#implicit-variant-syntax","level":2,"title":"Implicit variant syntax","text":"<p>Inside a <code>match</code> arm, the enum name can be omitted when the type is unambiguous:</p> <pre><code>match d {\n    .North -&gt; print(\"north\")\n    .South -&gt; print(\"south\")\n    else -&gt; {}\n}\n</code></pre> <p>Unit variants can also use the implicit <code>.Variant</code> form in expressions:</p> <pre><code>let d: Direction = .North\n</code></pre> <p>Payload variants always require the explicit form at construction time.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#pattern-matching","level":2,"title":"Pattern matching","text":"<pre><code>match m {\n    Message.Quit -&gt; print(\"quit\")\n    Message.Text(content) -&gt; print(content)\n    Message.Move(x, y) -&gt; {\n        print_int(x)\n        print_int(y)\n    }\n    else -&gt; {}\n}\n</code></pre> <ul> <li>Bindings in payload patterns (<code>content</code>, <code>x</code>, <code>y</code>) capture the field values</li> <li><code>else</code> is the catch-all arm; matches whatever the other arms didn't</li> <li>Arm bodies can be a single expression (after <code>-&gt;</code>) or a block (<code>{ ... }</code>)</li> </ul>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#memory-layout-compiler","level":2,"title":"Memory layout (compiler)","text":"<pre><code>[ offset 0  ] discriminant : int64  (8 bytes)\n[ offset 8  ] payload      : max(variant payload sizes) bytes\n</code></pre> <p>Variants are assigned discriminants in declaration order starting from 0.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#example-message","level":3,"title":"Example: <code>Message</code>","text":"<pre><code>Message.Quit  → discriminant 0, no payload\nMessage.Text  → discriminant 1, content at payload offset 0  (8 bytes)\nMessage.Move  → discriminant 2, x at offset 0, y at offset 8 (16 bytes)\n</code></pre> <p>Total size: 8 (discriminant) + 16 (max payload) = 24 bytes.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#equality","level":2,"title":"Equality","text":"<p>Two enum values are equal if and only if they are the same variant. Payload contents are not compared.</p> <pre><code>Direction.North == Direction.North  // true\nDirection.North == Direction.South  // false\n</code></pre> <p>In the compiler this is a single integer comparison on the discriminant.</p>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#match-as-an-expression","level":2,"title":"<code>match</code> as an expression","text":"<p>A match can produce a value when every arm body is an expression:</p> <pre><code>let label: str = match d {\n    .North -&gt; \"north\"\n    .South -&gt; \"south\"\n    .East  -&gt; \"east\"\n    .West  -&gt; \"west\"\n}\n</code></pre>","path":["Language guide","Enums"],"tags":[]},{"location":"enums/#current-limitations","level":2,"title":"Current limitations","text":"<ul> <li>No nested patterns (e.g. <code>Shape.Rect(w, 0)</code> matching on a specific field value).</li> <li>No guard clauses on match arms.</li> </ul>","path":["Language guide","Enums"],"tags":[]},{"location":"functions/","level":1,"title":"Functions","text":"","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#declaration","level":2,"title":"Declaration","text":"<pre><code>func add(a: int64, b: int64) -&gt; int64 {\n    return a + b\n}\n</code></pre> <p>Parameters are <code>name: Type</code>. The return type follows <code>-&gt;</code>. Functions that return nothing use <code>-&gt; none</code>.</p>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#calling","level":2,"title":"Calling","text":"<pre><code>let result: int64 = add(1, 2)\n</code></pre>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#call-labels","level":2,"title":"Call labels","text":"<p>Calls use labeled arguments by default; the parameter name is the label:</p> <pre><code>func greet(name: str) -&gt; none {\n    print(f\"Hello, {name}!\")\n}\n\ngreet(name: \"Alice\")\n</code></pre> <p><code>anon</code> removes the label requirement for that parameter:</p> <pre><code>func double(anon n: int64) -&gt; int64 {\n    return n * 2\n}\n\ndouble(5)\n</code></pre> <p>Convention: when calling, label every parameter except 1–2 of a single shared type (e.g. <code>slice(arr, from: 0, to: 3)</code> is fine; passing a label for <code>arr</code> is just noise).</p> <p>If a passed argument is a bare identifier with the same name as the parameter, the label can be elided:</p> <pre><code>let name: str = \"Alice\"\ngreet(name)           // shorthand for greet(name: name)\n</code></pre>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#label-binding-split","level":2,"title":"Label / binding split","text":"<p>A parameter can have two names — an external label used at call sites and an internal binding used inside the function body:</p> <pre><code>func greet(who person: str) -&gt; none {\n    print(f\"Hello, {person}!\")\n}\n\ngreet(who: \"Alice\")\n</code></pre> <p>Useful for prepositions that make call sites read naturally without forcing short variable names inside the function.</p>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#main","level":2,"title":"<code>main</code>","text":"<p>Every executable must define <code>main</code>. It can take no arguments or receive the command-line argument list:</p> <pre><code>// no args\nfunc main() -&gt; int64 {\n    return 0\n}\n\n// with argv\nfunc main(argv: [str]) -&gt; int64 {\n    print_int(len(argv))\n    return 0\n}\n</code></pre>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#methods","level":2,"title":"Methods","text":"<p>Functions inside a <code>struct</code> type declaration are methods. The first parameter must be <code>self</code>:</p> <pre><code>type Counter = struct {\n    value: int64\n\n    func increment(self) -&gt; none {\n        self.value = self.value + 1\n    }\n\n    func get(self) -&gt; int64 {\n        return self.value\n    }\n}\n</code></pre> <p>Call with dot notation:</p> <pre><code>let c: Counter = Counter(value: 0)\nc.increment()\nprint_int(c.get())\n</code></pre>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#function-pointer-types","level":2,"title":"Function-pointer types","text":"<p>Functions are values. Use <code>func(T1, T2) -&gt; R</code> as a type to store, pass, or return them — the runtime representation is an 8-byte pointer to the function's code, same as a C function pointer.</p> <pre><code>func double(x: int64) -&gt; int64 { return x * 2 }\nfunc triple(x: int64) -&gt; int64 { return x * 3 }\n\nfunc apply(anon f: func(int64) -&gt; int64, anon x: int64) -&gt; int64 {\n    return f(x)\n}\n\nlet g: func(int64) -&gt; int64 = double\nprint_int(g(21))             // 42 — indirect call through g\nprint_int(apply(triple, 14)) // 42 — function passed as callback\n</code></pre> <p>Plain named functions only — no anonymous functions (lambdas) or closures yet. A bare identifier in value position resolves to the function's address when it isn't shadowed by a local variable; a call site <code>f(args)</code> does an indirect <code>blr</code> when <code>f</code> is a local of function type, falling through to the direct <code>bl</code> otherwise.</p>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#extern-functions","level":2,"title":"Extern functions","text":"<p>Declare external C functions with <code>extern</code>:</p> <pre><code>extern func strlen(anon s: str) -&gt; int64\nextern func putchar(anon c: int64) -&gt; int64\n</code></pre> <p>These link against the C runtime and any object files passed at link time.</p>","path":["Language guide","Functions"],"tags":[]},{"location":"functions/#recursion","level":2,"title":"Recursion","text":"<p>Recursion works in the interpreter. In the compiler, mutual recursion between functions is supported as long as all functions are declared in the same module.</p> <pre><code>func fib(n: int64) -&gt; int64 {\n    if n &lt;= 1 {\n        return n\n    }\n    return fib(n - 1) + fib(n - 2)\n}\n</code></pre>","path":["Language guide","Functions"],"tags":[]},{"location":"install/","level":1,"title":"Install","text":"","path":["Install"],"tags":[]},{"location":"install/#requirements","level":2,"title":"Requirements","text":"<ul> <li>macOS on Apple Silicon (AArch64)</li> <li>Python 3.13+</li> <li>uv (recommended)</li> <li>Xcode Command Line Tools (<code>xcode-select --install</code>) — provides <code>as</code>, <code>ld</code>, and <code>clang</code></li> </ul>","path":["Install"],"tags":[]},{"location":"install/#from-source","level":2,"title":"From source","text":"<pre><code>git clone https://codeberg.org/filipsalo/kod\ncd kod\nuv sync\n</code></pre>","path":["Install"],"tags":[]},{"location":"install/#building-the-self-hosted-compiler","level":2,"title":"Building the self-hosted compiler","text":"<p>The Makefile bootstraps everything from scratch:</p> <pre><code>make bootstrap   # clean rebuild of stage0 + stage1 (sh_kodc)\nmake stage1      # incremental rebuild\nmake test        # run pytest\n</code></pre>","path":["Install"],"tags":[]},{"location":"install/#running","level":2,"title":"Running","text":"<p><code>kod</code> is installed as a <code>uv run</code> script:</p> <pre><code>uv run kod build hello.kod   # compile to a native binary\nuv run kod run hello.kod     # build + execute\nuv run kod test hello.kod    # run any `test \"...\" {}` blocks\nuv run kod check hello.kod   # parse + codegen; report errors but emit nothing\n</code></pre> <p>For a global install:</p> <pre><code>uv tool install .   # puts `kod` on your PATH\n</code></pre> <p>See CLI reference for the full command surface.</p>","path":["Install"],"tags":[]},{"location":"json/","level":1,"title":"JSON","text":"<p><code>stdlib/json.kod</code> parses and emits JSON.</p>","path":["Reference","JSON"],"tags":[]},{"location":"json/#quick-example","level":2,"title":"Quick example","text":"<pre><code>import \"json\"\n\nlet v: json.Value = json.parse(\"[1, 2, 3]\")\n// v is Array([Number(1), Number(2), Number(3)])\n\nlet s: str = json.emit(v)\n// s is \"[1,2,3]\"\n</code></pre>","path":["Reference","JSON"],"tags":[]},{"location":"json/#the-value-type","level":2,"title":"The <code>Value</code> type","text":"<pre><code>type Value = enum {\n    Null\n    Bool(value: bool)\n    Number(value: int64)        // int64 only — floats not supported\n    Str(value: str)\n    Array(items: [Value])\n    Object(keys: [str], values: [Value])\n}\n</code></pre> <p>Object entries are stored as parallel arrays (<code>keys[i]</code> ↔ <code>values[i]</code>) rather than a separate <code>Entry</code> struct, so the type stays single-recursive.</p>","path":["Reference","JSON"],"tags":[]},{"location":"json/#accessors","level":2,"title":"Accessors","text":"<p>Free functions (Kod doesn't yet support methods on enums) that return <code>T?</code> so callers can chain via <code>let .Some(v) = ... else</code>:</p> Function Returns <code>get(v: Value, key: str) -&gt; Value</code> Object lookup. Returns <code>Null</code> if the key is missing or <code>v</code> isn't an Object. <code>as_str(v: Value) -&gt; str?</code> Unwrap to a string, <code>none</code> if not <code>Str</code>. <code>as_int(v: Value) -&gt; int64?</code> Unwrap to an integer, <code>none</code> if not <code>Number</code>. <code>as_bool(v: Value) -&gt; bool?</code> Unwrap to a bool, <code>none</code> if not <code>Bool</code>. <pre><code>let .Some(method) = json.as_str(json.get(req, key: \"method\")) else { return }\nlet .Some(id) = json.as_int(json.get(req, key: \"id\")) else { return }\n</code></pre>","path":["Reference","JSON"],"tags":[]},{"location":"json/#limitations","level":2,"title":"Limitations","text":"<ul> <li>Numbers are int64 only.</li> <li>String escapes: <code>\\n \\t \\r \\\" \\\\ \\/</code>. No <code>\\uXXXX</code>.</li> <li>Strict JSON: no trailing commas, no comments.</li> <li>Parse failure currently panics rather than returning an error type.</li> </ul>","path":["Reference","JSON"],"tags":[]},{"location":"memory-layout/","level":1,"title":"Memory Layout","text":"<p>This document describes how Kod values are represented in memory at runtime.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#primitive-types","level":2,"title":"Primitive types","text":"<p>Primitive values are stored directly — on the stack in local variables, or passed by value in registers.</p> Type Width Representation <code>int64</code> 8 Two's-complement 64-bit int <code>str</code> 8 Pointer to null-terminated C string in <code>.data</code> section <code>bool</code> 1 0 (false) or 1 (true) <code>none</code> 8 Always 0 (null sentinel)","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#struct-values","level":2,"title":"Struct values","text":"<p>Struct variables are reference types: the variable holds an 8-byte pointer to the struct's data, which lives in the global arena.</p> <pre><code>stack slot (8 bytes)          arena memory\n┌──────────────┐              ┌──────────────┐\n│   pointer ───┼─────────────▶│   field 0    │  offset 0\n└──────────────┘              │   field 1    │  offset 8\n                              │   ...        │\n                              └──────────────┘\n</code></pre> <p>Fields are laid out sequentially in declaration order, each at its natural alignment. The total allocation size (<code>data_width</code>) is the sum of all field widths.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#example-point","level":3,"title":"Example: <code>Point</code>","text":"<pre><code>type Point = struct {\n    x: int64    // offset 0, width 8\n    y: int64    // offset 8, width 8\n}\n</code></pre> <ul> <li><code>data_width</code> = 16 (allocated in arena)</li> <li><code>width</code> = 8 (size of the pointer stored in a stack slot)</li> </ul>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#reference-semantics","level":3,"title":"Reference semantics","text":"<p>Assigning a struct variable copies the pointer, not the data:</p> <pre><code>let p = Point(x: 3, y: 4)\nlet q: Point = p        // q and p point to the same object\nq.x = 10               // also modifies p.x\n</code></pre>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#enum-values","level":2,"title":"Enum values","text":"<p>Enums are arena-allocated, like structs. A stack slot holds an 8-byte pointer; the cell at the pointer is:</p> <pre><code>[offset 0]  discriminant : int64  (variant index starting from 0)\n[offset 8]  payload      : max(variant payload sizes) bytes\n</code></pre> <p>Because enum variables are pointers, they fit in a single register and pass through the ARM64 calling convention without any multi-word ABI gymnastics.</p> <p>See <code>docs/enums.md</code> for details.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#the-global-arena","level":2,"title":"The global arena","text":"<p>All struct allocations go into a single implicit arena, backed by a linked-list bump allocator in <code>stdlib/arena.c</code>. The user never interacts with the arena directly — struct constructors allocate into it automatically.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#allocator-design","level":3,"title":"Allocator design","text":"<ul> <li>Initial block: 4 MB (allocated via <code>malloc</code>)</li> <li>When a block is full, a new block of at least 4 MB is linked in</li> <li>Allocations are aligned to 8 bytes</li> <li>Memory is never freed during a program run (the OS reclaims it on exit)</li> </ul> <p>This is appropriate for compiler workloads, where all data is allocated upfront and the process exits when done.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#entry-point","level":3,"title":"Entry point","text":"<pre><code>void *arena_alloc(int64_t size);\n</code></pre> <p>Called internally by the compiler whenever a struct is constructed. Not exposed to Kod programs directly.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"memory-layout/#calling-convention-arm64-macos","level":2,"title":"Calling convention (ARM64, macOS)","text":"<ul> <li>Arguments: passed in <code>x0</code>–<code>x7</code></li> <li>Return value: in <code>x0</code></li> <li>Caller-saved: <code>x0</code>–<code>x15</code></li> <li>Callee-saved: <code>x19</code>–<code>x28</code></li> <li>Frame pointer: <code>fp</code> (<code>x29</code>)</li> <li>Link register: <code>lr</code> (<code>x30</code>)</li> </ul> <p>Since struct variables are 8-byte pointers, they fit in a single register and can be passed to and returned from functions normally.</p>","path":["Reference","Memory Layout"],"tags":[]},{"location":"modules/","level":1,"title":"Modules","text":"<p>Each <code>.kod</code> file is a module. Modules are imported by path, relative to the project root.</p>","path":["Language guide","Modules"],"tags":[]},{"location":"modules/#importing","level":2,"title":"Importing","text":"<p>Two forms:</p> <pre><code>import \"./utils\"        // relative — utils.kod next to the importing file\nimport \"./math/vec\"     // relative — math/vec.kod in a sibling directory\nimport \"kod/ast\"        // stdlib — stdlib/kod/ast.kod\nimport \"io\"             // stdlib — stdlib/io.kod\n</code></pre> <p>Paths beginning with <code>./</code> resolve relative to the importing file. Anything else resolves under <code>stdlib/</code>.</p> <p>The imported module's public names are accessible via the import alias (the last path component by default):</p> <pre><code>import \"./math/vec\"\n\nlet v: vec.Vec2 = vec.Vec2(x: 1, y: 2)\n</code></pre>","path":["Language guide","Modules"],"tags":[]},{"location":"modules/#standard-library","level":2,"title":"Standard library","text":"<p>The stdlib lives under <code>stdlib/</code> in the repository.</p> Module Contents <code>builtins</code> Always imported. <code>print</code>, <code>print_int</code>, <code>len</code>, <code>fail</code>, <code>Map[K, V]</code>, the <code>Error</code> / <code>Stringable</code> / <code>ExitCode</code> interfaces. <code>primitives/int64</code>, <code>primitives/str</code>, <code>primitives/bool</code> Methods on the primitive types (<code>to_str</code>, <code>hash</code>). Auto-loaded; not imported directly. <code>io</code> <code>read_file</code>. <code>process</code> <code>process.run(argv)</code> — spawn a subprocess and capture stdout/stderr/exit code. <code>kod/ast</code> AST node types (for the self-hosted compiler). <code>kod/lexing</code> Tokenizer (self-hosted). <code>kod/parsing</code> Parser (self-hosted). <code>kod/codegen</code> ARM64 code generator.","path":["Language guide","Modules"],"tags":[]},{"location":"modules/#circular-imports","level":2,"title":"Circular imports","text":"<p>Circular imports are not supported. The dependency graph must be a DAG.</p>","path":["Language guide","Modules"],"tags":[]},{"location":"modules/#visibility","level":2,"title":"Visibility","text":"<p>All top-level declarations (functions, types, variables) are visible to importing modules. There is no explicit <code>pub</code>/<code>private</code> distinction yet.</p>","path":["Language guide","Modules"],"tags":[]},{"location":"pattern-matching/","level":1,"title":"Pattern matching","text":"<p>Kod's <code>match</code> statement dispatches on a value and binds sub-parts.</p>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#syntax","level":2,"title":"Syntax","text":"<pre><code>match &lt;expression&gt; {\n    &lt;pattern&gt; -&gt; &lt;arm-body&gt;\n    &lt;pattern&gt; -&gt; {\n        // multi-statement arm\n    }\n}\n</code></pre> <p>Arms are tested top-to-bottom. The first matching arm executes.</p>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#pattern-kinds","level":2,"title":"Pattern kinds","text":"","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#catch-all-else","level":3,"title":"Catch-all (<code>else</code>)","text":"<p><code>else</code> matches any value the other arms didn't cover. It must be the last arm. The arrow is optional after <code>else</code>:</p> <pre><code>match x {\n    1    -&gt; print(\"one\")\n    2    -&gt; print(\"two\")\n    else -&gt; print(\"other\")    // arrow form\n}\n\nmatch x {\n    1 -&gt; print(\"one\")\n    else { print(\"other\") }   // block form\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#integer-literal","level":3,"title":"Integer literal","text":"<pre><code>match n {\n    0    -&gt; print(\"zero\")\n    1    -&gt; print(\"one\")\n    else -&gt; print(\"other\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#string-literal","level":3,"title":"String literal","text":"<pre><code>match s {\n    \"hello\" -&gt; print(\"greeting\")\n    \"bye\"   -&gt; print(\"farewell\")\n    else    -&gt; print(\"unknown\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#bool","level":3,"title":"Bool","text":"<pre><code>match b {\n    true  -&gt; print(\"yes\")\n    false -&gt; print(\"no\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#enum-variant-explicit","level":3,"title":"Enum variant (explicit)","text":"<pre><code>type Color = enum { Red, Green, Blue }\n\nmatch c {\n    Color.Red   -&gt; print(\"red\")\n    Color.Green -&gt; print(\"green\")\n    Color.Blue  -&gt; print(\"blue\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#enum-variant-implicit-shorthand","level":3,"title":"Enum variant (implicit shorthand)","text":"<p>When the type is clear from context, the enum name can be omitted:</p> <pre><code>match c {\n    .Red   -&gt; print(\"red\")\n    .Green -&gt; print(\"green\")\n    .Blue  -&gt; print(\"blue\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#payload-variant","level":3,"title":"Payload variant","text":"<p>Bind the payload fields:</p> <pre><code>type Shape = enum {\n    Circle(radius: int64)\n    Rect(w: int64, h: int64)\n}\n\nmatch shape {\n    Shape.Circle(radius) -&gt; print_int(radius)\n    Shape.Rect(w, h)     -&gt; print_int(w * h)\n}\n</code></pre> <p>Bindings are by position — the names in the pattern don't have to match the field names.</p>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#optional","level":3,"title":"Optional","text":"<pre><code>match maybe_n {\n    Some(n) -&gt; print_int(n)\n    none    -&gt; print(\"nothing\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#is-test","level":3,"title":"<code>is</code> test","text":"<p>A lightweight check without destructuring:</p> <pre><code>if x is none {\n    print(\"empty\")\n}\nif x is not none {\n    print(\"has value\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#match-as-expression","level":2,"title":"Match as expression","text":"<p>A <code>match</code> produces a value when every arm body is a single expression:</p> <pre><code>let label: str = match direction {\n    .North -&gt; \"north\"\n    .South -&gt; \"south\"\n    .East  -&gt; \"east\"\n    .West  -&gt; \"west\"\n}\n</code></pre> <p>For multi-statement arm bodies, use a match statement and assign or return from inside.</p>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#if-x-is-variantbindings","level":2,"title":"<code>if X is .Variant(bindings) { ... }</code>","text":"<p>When you want a one-arm match with bindings in scope for the body, the <code>if</code>/<code>is</code> sugar is shorter than a full <code>match</code>:</p> <pre><code>if module.decls[i] is .Func(decl_id) {\n    // decl_id is in scope here\n}\n</code></pre> <p>An optional <code>else { ... }</code> runs when the pattern doesn't match. Pattern bindings stay scoped to the matching arm — they aren't in scope in the <code>else</code> block.</p> <pre><code>if v is .Some(x) {\n    print_int(x)\n} else {\n    print(\"missing\")\n}\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#let-patternbindings-expr-else","level":2,"title":"<code>let .Pattern(bindings) = expr else { ... }</code>","text":"<p>Destructure with an early-exit on the non-matching case. The <code>else</code> block must exit the enclosing scope (return / throw / panic) so the bindings are guaranteed-in-scope below:</p> <pre><code>let .Some(v) = m.get(key) else { return -1 }\n// v is in scope here\n</code></pre>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#exhaustiveness","level":2,"title":"Exhaustiveness","text":"<p>A <code>match</code> on an enum (or on <code>T?</code> or <code>bool</code>) must cover every variant. The compiler reports an error if any variant is missing and no wildcard arm is present.</p> <pre><code>type Color = enum { Red, Green, Blue }\n\nlet c: Color = .Red\n// error: match on Color doesn't cover variant Blue\nmatch c {\n    .Red -&gt; print(\"red\")\n    .Green -&gt; print(\"green\")\n}\n</code></pre> <p>Add the missing arm or an <code>else</code> catch-all:</p> <pre><code>match c {\n    .Red -&gt; print(\"red\")\n    .Green -&gt; print(\"green\")\n    else -&gt; print(\"other\")\n}\n</code></pre> <p>The single-arm sugar (<code>if X is .V(b) { ... }</code> and <code>let .V(b) = X else { ... }</code>) is not subject to exhaustiveness — they're explicitly one-variant checks.</p> <p>A <code>match</code> on <code>int64</code> or <code>str</code> must include an <code>else</code> arm — the compiler reports an error otherwise. (<code>int64</code> and <code>str</code> have too many possible values to enumerate; the <code>else</code> makes the fall-through explicit.)</p>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"pattern-matching/#other-limitations","level":2,"title":"Other limitations","text":"<ul> <li>No nested patterns (e.g. <code>Shape.Rect(w, 0)</code> matching on a specific field value)</li> <li>No guard clauses (<code>if</code> conditions on arms)</li> </ul>","path":["Language guide","Pattern matching"],"tags":[]},{"location":"quickstart/","level":1,"title":"Quickstart","text":"","path":["Quickstart"],"tags":[]},{"location":"quickstart/#hello-world","level":2,"title":"Hello, world","text":"<p>Create <code>hello.kod</code>:</p> <pre><code>func main() -&gt; int64 {\n    print(\"Hello, world!\")\n    return 0\n}\n</code></pre> <p>Build and run:</p> <pre><code>$ uv run kod build hello.kod\n$ ./build/apps/hello/hello\nHello, world!\n</code></pre> <p><code>uv run kod run hello.kod</code> does both in one step.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#a-real-program","level":2,"title":"A real program","text":"<pre><code>func greet(anon name: str) -&gt; none {\n    print(f\"Hello, {name}!\")\n}\n\nfunc main() -&gt; int64 {\n    let names: [str] = [\"Alice\", \"Bob\", \"Carol\"]\n    for name in names {\n        greet(name)\n    }\n    return 0\n}\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#variables","level":2,"title":"Variables","text":"<p>Bindings are declared with <code>let</code> and are immutable — the name can't be reassigned. A type annotation is usually required:</p> <pre><code>let x: int64 = 42\nlet s: str = \"hello\"\nlet flag: bool = true\n</code></pre> <p>For a binding you intend to reassign, use <code>mut</code>:</p> <pre><code>mut count: int64 = 0\ncount = count + 1   // ok\n\nlet limit: int64 = 10\nlimit = 20          // error: cannot reassign immutable binding `limit`\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#a-test-file","level":2,"title":"A test file","text":"<pre><code>// arithmetic_test.kod\ntest \"addition\" {\n    assert 2 + 2 == 4\n}\n\ntest \"subtraction\" {\n    assert 10 - 3 == 7\n}\n</code></pre> <pre><code>$ uv run kod test arithmetic_test.kod\nok   addition\nok   subtraction\n\n2/2 passed\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Types — the full type system</li> <li>Functions — declaration, parameters, return types</li> <li>Control flow — <code>if</code>, <code>for</code>, <code>match</code></li> <li>Testing — the <code>test</code> and <code>assert</code> syntax</li> </ul>","path":["Quickstart"],"tags":[]},{"location":"self-hosting/","level":1,"title":"Self-hosting","text":"<p>The primary goal of Kod is self-hosting — a compiler written in Kod that compiles itself. The Python frontend exists to bootstrap that, and is shrinking as more orchestration moves into Kod.</p>","path":["Self-hosting"],"tags":[]},{"location":"self-hosting/#status","level":2,"title":"Status","text":"<p>The self-hosted compiler <code>sh_kodc</code> exists and is the production path. <code>make stage1</code> builds it, and every <code>kod build</code> / <code>kod run</code> / <code>kod test</code> invocation uses it for codegen when available.</p> <p>Sources in Kod:</p> File Role <code>stdlib/kod/lexing.kod</code> Tokenizer <code>stdlib/kod/parsing.kod</code> Parser (AST built into a pool of <code>int64</code> ids) <code>stdlib/kod/ast.kod</code> AST node types <code>stdlib/kod/codegen.kod</code> ARM64 assembly emitter <code>kodc.kod</code> Driver: parses entry + imports, registers types, runs codegen <code>stdlib/process.kod</code>, <code>stdlib/io.kod</code>, <code>stdlib/testing.kod</code> Stdlib <p><code>sh_kodc</code> reads a <code>.kod</code> file and emits ARM64 assembly to stdout, plus a few internal subcommands (<code>_emit-runtime-main</code>) that grow over time as build steps move out of Python.</p>","path":["Self-hosting"],"tags":[]},{"location":"self-hosting/#build-chain","level":2,"title":"Build chain","text":"<pre><code>build/stage0/   shared stdlib objects (arena.o, runtime.o, _builtins.o,\n                _int64.o, _str.o, _bool.o)\nbuild/stage1/   sh_kodc + its .s/.o, plus runtime_main.{s,o}\nbuild/apps/&lt;stem&gt;/  per-app outputs + the final executable\n</code></pre> <p>Bootstrap, driven by the <code>Makefile</code>:</p> <ol> <li>stage0 — the Python interpreter compiles every stdlib module under <code>stdlib/</code> to assembly, then assembles them. Slow but only happens once per source change.</li> <li>stage1 — for each of <code>lexing</code>, <code>parsing</code>, <code>codegen</code>, <code>kodc</code>, the Python interpreter drives <code>kodc.kod</code> to emit <code>.s</code>, which <code>as</code> assembles. <code>_emit-runtime-main</code> writes the <code>_main</code> shim. <code>ld</code> links it all into <code>build/stage1/sh_kodc</code>.</li> <li>After bootstrap, <code>kod build foo.kod</code> uses <code>sh_kodc</code> to compile each module; the Python interpreter is only used as a fallback if <code>sh_kodc</code> is missing or stale.</li> </ol>","path":["Self-hosting"],"tags":[]},{"location":"self-hosting/#whats-still-python","level":2,"title":"What's still Python","text":"<ul> <li>The build orchestrator (<code>kod/builder.py</code>) drives <code>as</code> / <code>ld</code> / <code>clang</code> and walks the module graph. Subprocess support in Kod (<code>process.run</code> in <code>stdlib/process.kod</code>) is the foundation for moving this in; the first step landed (<code>_emit-runtime-main</code> is Kod-native).</li> <li>The Python lexer/parser still exists, used by <code>kod _interpret</code> and as the entry point that walks imports. <code>kod build</code> parses with Python then hands each module to <code>sh_kodc</code>; the parse happens twice in practice.</li> </ul> <p>See <code>TODO.md</code> for the running list of Python-deletion work.</p>","path":["Self-hosting"],"tags":[]},{"location":"self-hosting/#codegen-design","level":2,"title":"Codegen design","text":"<p><code>codegen.kod</code> is a single-pass emitter built around a few key data structures:</p> <ul> <li>Expression / statement / type-expr pools on the parser: AST nodes are stored in flat arrays and referenced by <code>int64</code> ids.</li> <li>Struct / enum / interface tables on the codegen: registration happens before compilation; field/method lookups index these tables.</li> <li>Register pool: <code>x8</code>–<code>x15</code> used as expression temporaries; saved/restored across calls via stack spills as needed.</li> <li>Generic monomorphisation: each unique <code>Map[str, int64]</code> instantiation gets its own struct entry; methods compile once per instantiation.</li> <li>Interface dispatch: <code>{data_ptr, vtable_ptr}</code> cells in the arena, one vtable per (struct, interface) pair; primitives box implicitly.</li> </ul>","path":["Self-hosting"],"tags":[]},{"location":"self-hosting/#verifying-self-hosting","level":2,"title":"Verifying self-hosting","text":"<pre><code>$ make bootstrap\n$ build/stage1/sh_kodc tests/map.kod &gt; /tmp/sh.s\n$ uv run kod _interpret kodc.kod tests/map.kod &gt; /tmp/py.s\n$ diff /tmp/sh.s /tmp/py.s\n</code></pre> <p><code>sh_kodc</code> and the interpreter-driven <code>kodc.kod</code> produce identical assembly. <code>tests/examples_test.py::test_selfhost_matches_interpreter</code> runs this check in CI.</p>","path":["Self-hosting"],"tags":[]},{"location":"strings/","level":1,"title":"Strings","text":"<p>Strings in Kod are UTF-8 byte sequences. The type is <code>str</code>.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#literals","level":2,"title":"Literals","text":"<pre><code>let s: str = \"hello, world\"\n</code></pre> <p>Standard escape sequences are supported: <code>\\n</code>, <code>\\t</code>, <code>\\\\</code>.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#f-strings","level":2,"title":"F-strings","text":"<p>Interpolate expressions into strings with <code>f\"...\"</code>:</p> <pre><code>let name: str = \"world\"\nprint(f\"hello, {name}!\")          // hello, world!\n\nlet n: int64 = 42\nprint(f\"the answer is {n}\")       // the answer is 42\n</code></pre> <p>Anything inside <code>{ }</code> is a Kod expression. Non-<code>str</code> values are auto-converted via their <code>to_str</code> method:</p> <pre><code>print(f\"{a} + {b} = {a + b}\")\nprint(f\"flag={true}\")             // flag=true\nprint(f\"items: {items.len()}\")    // any type with to_str works\n</code></pre>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#concatenation","level":2,"title":"Concatenation","text":"<p>Use <code>+</code> to concatenate strings, or <code>+=</code> to append in place:</p> <pre><code>let greeting: str = \"hello\" + \", \" + name + \"!\"\n\nlet buf: str = \"lines:\\n\"\nbuf += \"  one\\n\"\nbuf += \"  two\\n\"\n</code></pre> <p><code>+=</code> works on struct fields too: <code>obj.message += \" (cont.)\"</code>.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#multi-line-strings","level":2,"title":"Multi-line strings","text":"<p>Triple-quoted strings span multiple lines:</p> <pre><code>let preamble: str = \"\"\"\n    .text\n    .globl _main\n    _main:\n    \"\"\"\n</code></pre> <p>The closing <code>\"\"\"</code> must sit on its own line. Its indent anchors the dedent — every content line has at least that many leading spaces (or tabs) stripped. The newline immediately after the opening <code>\"\"\"</code> is not part of the value; the newline before the closing <code>\"\"\"</code> is. So the example above produces:</p> <pre><code>.text\n.globl _main\n_main:\n</code></pre> <p>(four-space indent gone; trailing newline kept).</p> <p>Combine with <code>f</code> for interpolation:</p> <pre><code>let asm: str = f\"\"\"\n    bl ${mangled}$main\n    ret\n    \"\"\"\n</code></pre> <p>Escape sequences (<code>\\n</code>, <code>\\t</code>, <code>\\\\</code>) work the same as regular strings. Each content line must be indented at least as far as the closing <code>\"\"\"</code>; a content line with less indent is a lex error.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#length","level":2,"title":"Length","text":"<pre><code>let n: int64 = len(s)\n</code></pre>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#character-access","level":2,"title":"Character access","text":"<p>Index into a string to get the ASCII value of a character (as <code>int64</code>):</p> <pre><code>let c: int64 = s[0]   // 'h' → 104\n</code></pre> <p>Negative indices count from the end:</p> <pre><code>let last: int64 = s[-1]\n</code></pre>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#slicing","level":2,"title":"Slicing","text":"<pre><code>let sub: str = s[2..5]   // bytes at positions 2, 3, 4\n</code></pre> <p>Either endpoint may be omitted: <code>s[..k]</code> is <code>s[0..k]</code> and <code>s[k..]</code> is <code>s[k..len(s)]</code>. <code>s[..]</code> returns a copy.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#comparison","level":2,"title":"Comparison","text":"<pre><code>if s == \"hello\" {\n    print(\"match\")\n}\nif s != \"bye\" {\n    print(\"not bye\")\n}\n</code></pre> <p>String equality compares contents, not identity.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"strings/#conversion","level":2,"title":"Conversion","text":"<p>Primitives have a <code>to_str</code> method:</p> <pre><code>let s: str = 42.to_str()           // \"42\"\nlet t: str = true.to_str()         // \"true\"\nlet u: str = \"hi\".to_str()         // identity\n</code></pre> <p>Structs can opt in by declaring <code>func to_str(self) -&gt; str</code>. F-string interpolation calls <code>to_str</code> automatically.</p> <p>See Built-ins for the full list of string-related functions.</p>","path":["Language guide","Strings"],"tags":[]},{"location":"structs/","level":1,"title":"Structs","text":"<p>Structs are named product types — a fixed set of named fields.</p>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#declaration","level":2,"title":"Declaration","text":"<pre><code>type Point = struct {\n    x: int64\n    y: int64\n}\n</code></pre> <p>Comments are allowed inside the body:</p> <pre><code>type Rect = struct {\n    // top-left corner\n    x: int64\n    y: int64\n    // dimensions\n    width: int64\n    height: int64\n}\n</code></pre>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#construction","level":2,"title":"Construction","text":"<p>Pass fields by name using labeled arguments:</p> <pre><code>let p: Point = Point(x: 10, y: 20)\n</code></pre> <p>Every field must be supplied unless it has a default. Omitting a field with no default is a compile error — there's no implicit zero/null value:</p> <pre><code>let p: Point = Point(x: 10)   // error: struct `Point` requires field `y`\n</code></pre>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#field-defaults","level":2,"title":"Field defaults","text":"<p>A field may declare a default with <code>= expr</code>. When a construction omits that field, the default expression is evaluated and stored:</p> <pre><code>type Counter = struct {\n    label: str\n    count: int64 = 0\n    history: [int64] = []\n}\n\nlet c: Counter = Counter(label: \"hits\")   // count = 0, history = []\nlet d: Counter = Counter(label: \"x\", count: 5)\n</code></pre> <p>The default is evaluated fresh at each construction (so a <code>[]</code> default yields a new empty array per instance, not a shared one). Defaults are ordinary expressions evaluated in the struct's defining module.</p> <p>This is what lets the built-in <code>Map[K, V]</code> be created with no arguments — all three of its fields default to <code>[]</code>:</p> <pre><code>let m: Map[str, int64] = Map[str, int64]()\n</code></pre>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#field-access","level":2,"title":"Field access","text":"<pre><code>print_int(p.x)\nprint_int(p.y)\n</code></pre>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#field-mutation","level":2,"title":"Field mutation","text":"<pre><code>p.x = p.x + 1\n</code></pre>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#methods","level":2,"title":"Methods","text":"<p>Define methods inside the struct body. The first parameter must be <code>self</code>:</p> <pre><code>type Point = struct {\n    x: int64\n    y: int64\n\n    func distance_sq(self) -&gt; int64 {\n        return self.x * self.x + self.y * self.y\n    }\n\n    func translate(self, dx: int64, dy: int64) -&gt; none {\n        self.x = self.x + dx\n        self.y = self.y + dy\n    }\n}\n</code></pre> <p>Call methods with dot notation:</p> <pre><code>let p: Point = Point(x: 3, y: 4)\nprint_int(p.distance_sq())   // 25\np.translate(dx: 1, dy: 0)\nprint_int(p.x)               // 4\n</code></pre> <p>Methods can also be invoked on non-<code>Ident</code> receivers — <code>0.to_str()</code>, <code>p.field.method()</code>, <code>f().bar()</code>, etc.</p>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#generic-structs","level":2,"title":"Generic structs","text":"<p>Type parameters in square brackets after the name; each unique instantiation is monomorphised:</p> <pre><code>type Pair[A, B] = struct {\n    first: A\n    second: B\n\n    func swap(self) -&gt; Pair[B, A] {\n        return Pair[B, A](first: self.second, second: self.first)\n    }\n}\n\nlet p: Pair[str, int64] = Pair[str, int64](first: \"hello\", second: 42)\n</code></pre> <p>The built-in <code>Map[K, V]</code> is the canonical example.</p>","path":["Language guide","Structs"],"tags":[]},{"location":"structs/#memory-layout-compiler","level":2,"title":"Memory layout (compiler)","text":"<p>Struct fields are laid out sequentially, each taking 8 bytes, allocated on an arena:</p> <pre><code>[ offset 0  ] field_0 : 8 bytes\n[ offset 8  ] field_1 : 8 bytes\n...\n</code></pre> <p>Struct variables hold a pointer to the arena allocation.</p>","path":["Language guide","Structs"],"tags":[]},{"location":"testing/","level":1,"title":"Testing","text":"<p>Kod has built-in syntax for unit tests: <code>test \"description\" { ... }</code> blocks and an <code>assert &lt;expr&gt;</code> statement. <code>kod test</code> runs them.</p>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#a-first-test","level":2,"title":"A first test","text":"<pre><code>// arithmetic_test.kod\ntest \"addition works\" {\n    assert 2 + 2 == 4\n}\n\ntest \"negative arithmetic\" {\n    assert -1 + 1 == 0\n    assert -5 - 5 == -10\n}\n</code></pre> <pre><code>$ uv run kod test arithmetic_test.kod\nok   addition works\nok   negative arithmetic\n\n2/2 passed\n</code></pre>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#assert-expr","level":2,"title":"<code>assert &lt;expr&gt;</code>","text":"<p>If the expression evaluates to <code>false</code>, the current test is marked failed and the source text of the expression is captured in the failure message — no need to type <code>f\"got {x}, want {y}\"</code>:</p> <pre><code>test \"this one will fail\" {\n    let n: int64 = 7\n    assert n == 4\n}\n</code></pre> <pre><code>    foo_test.kod:3:5: assertion failed: n == 4\nFAIL this one will fail\n</code></pre> <p>Multiple asserts per test continue running — they accumulate failure messages but don't short-circuit, so a single test surfaces every failing condition at once.</p> <p>Outside a <code>test</code> block, <code>assert</code> panics with the same message (exit 1). Same syntax, intent matches the surrounding context — like Rust's always-on <code>assert!</code>.</p>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#failmsg","level":2,"title":"<code>fail(msg)</code>","text":"<p>For failures that aren't a boolean check, call the builtin <code>fail</code>:</p> <pre><code>test \"map lookup hits\" {\n    let m: Map[str, int64] = Map[str, int64]()\n    m.set(\"a\", 1)\n    match m.get(\"a\") {\n        Some(v) -&gt; assert v == 1\n        none -&gt; fail(\"missing key after set\")\n    }\n}\n</code></pre> <p><code>fail</code> is a builtin — no import needed.</p>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#discovery","level":2,"title":"Discovery","text":"<ul> <li><code>kod test &lt;file&gt;</code> builds the file and every transitively imported module that contains <code>test</code> blocks, then runs them all in one binary.</li> <li><code>kod test &lt;dir&gt;</code> walks the directory for <code>*_test.kod</code>, builds them all in one binary via a temporary aggregator at the project root.</li> <li><code>kod test .</code> walks the entire project.</li> </ul> <p>The aggregate exit code is 0 if every test passed, non-zero otherwise.</p>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#layout-convention","level":2,"title":"Layout convention","text":"<p>Test files live next to the code they test, Go-style:</p> <pre><code>stdlib/\n  builtins.kod\n  map_test.kod\n  primitives/\n    int64.kod\n    int64_test.kod\n    str.kod\n    str_test.kod\n</code></pre> <p>The naming convention <code>*_test.kod</code> makes them auto-discoverable by <code>kod test &lt;dir&gt;</code> without showing up as importable modules.</p>","path":["Language guide","Testing"],"tags":[]},{"location":"testing/#how-it-works-briefly","level":2,"title":"How it works (briefly)","text":"<ul> <li>The parser turns <code>test \"...\" { ... }</code> into a <code>Decl.Test</code> carrying a synthesized <code>FuncDecl</code>.</li> <li>The codegen emits each test as a regular function (<code>&lt;prefix&gt;$__test_N</code>) and, per module, a <code>&lt;prefix&gt;$__run_tests</code> dispatcher.</li> <li><code>kod test</code> builds a runtime <code>_main</code> that calls every test-bearing module's <code>__run_tests</code> in turn, then <code>_kod_test_summary</code> once at the end. Counters live in C runtime statics.</li> <li><code>fail</code>/<code>assert</code> set a per-test \"failed\" flag the dispatcher reads after each test returns; that flag drives the <code>ok</code> / <code>FAIL</code> line and the aggregate count.</li> </ul>","path":["Language guide","Testing"],"tags":[]},{"location":"types/","level":1,"title":"Types","text":"<p>Kod is statically typed. Every variable and parameter requires a type annotation.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#bindings-and-mutability","level":2,"title":"Bindings and mutability","text":"<p>A <code>let</code> binding is immutable: once set, reassigning the name is a compile error. Use <code>mut</code> for a binding you need to reassign. <code>+=</code> and <code>-=</code> count as reassignment, so they require <code>mut</code> too.</p> <pre><code>let max: int64 = 100\nmut total: int64 = 0\ntotal += max          // ok — total is mutable\n\nmax = 50              // error: cannot reassign immutable binding `max`\n</code></pre> <p>Parameters are immutable too — <code>mut</code> makes one reassignable inside the body (<code>mut self</code> for a method receiver). This is callee-local: params are passed by value, so reassigning one never affects the caller, and the call site needs no annotation.</p> <pre><code>func bump(anon mut x: int64) -&gt; int64 {\n    x = x + 1     // ok — local to bump; the caller's value is unchanged\n    return x\n}\n</code></pre> <p>Struct fields stay mutable. And because structs and arrays are reference values (see Memory layout), mutating a struct's fields through a parameter or <code>self</code> (<code>p.field = v</code>) is still allowed regardless of whether the binding is <code>mut</code> — <code>mut</code> governs only reassigning the name.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#primitive-types","level":2,"title":"Primitive types","text":"Type Description Example <code>int64</code> 64-bit signed integer <code>42</code>, <code>-7</code> <code>str</code> UTF-8 string <code>\"hello\"</code> <code>bool</code> Boolean <code>true</code>, <code>false</code> <code>none</code> No value (unit type) —","path":["Language guide","Types"],"tags":[]},{"location":"types/#integer-literals","level":2,"title":"Integer literals","text":"<pre><code>let n: int64 = 42\nlet m: int64 = -1\n</code></pre> <p>Base prefixes for non-decimal forms:</p> <pre><code>let hex: int64 = 0xff           // 255\nlet oct: int64 = 0o755          // 493\nlet bin: int64 = 0b1010         // 10\n</code></pre> <p>Decimal literals may use a space as a thousands separator. The grouping is strict — a head of 1-3 digits followed by space-prefixed groups of exactly 3 digits each:</p> <pre><code>let pop: int64 = 1 000 000      // ok\nlet bad: int64 = 1 23 456       // lex error — middle group isn't 3 digits\n</code></pre> <p>Arithmetic operators: <code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>%</code>. Compound assignment <code>+=</code> and <code>-=</code> update a variable, field, or array element in place (<code>n -= 3</code> is <code>n = n - 3</code>).</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#boolean-literals","level":2,"title":"Boolean literals","text":"<pre><code>let flag: bool = true\nlet other: bool = false\n</code></pre> <p>Logical operators: <code>and</code>, <code>or</code>, <code>not</code> (prefix). Comparison: <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>, <code>&gt;=</code>.</p> <p>Chained comparisons are supported Python-style: <code>a &lt; b &lt; c</code> evaluates <code>b</code> once and is equivalent to <code>(a &lt; b) and (b &lt; c)</code>. Any comparison chain works — <code>0 &lt;= i &lt; len(xs)</code> is the canonical bounds check.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#none","level":2,"title":"<code>none</code>","text":"<p><code>none</code> is the unit value. Functions that return nothing are declared <code>-&gt; none</code>:</p> <pre><code>func say_hi() -&gt; none {\n    print(\"hi\")\n}\n</code></pre>","path":["Language guide","Types"],"tags":[]},{"location":"types/#optional-types","level":2,"title":"Optional types","text":"<p>Append <code>?</code> to any type to make it optional:</p> <pre><code>let x: int64? = none\nlet y: int64? = 42\n</code></pre> <p>Test with <code>is</code> and destructure with <code>Some(binding)</code>:</p> <pre><code>if x is none {\n    print(\"nothing\")\n}\nif y is not none {\n    print(\"got something\")\n}\nmatch y {\n    Some(v) -&gt; print_int(v)\n    none -&gt; print(\"nothing\")\n}\n</code></pre> <p><code>is not Pat</code> reads as one operator — <code>is</code> negated by the trailing <code>not</code> — and works anywhere <code>is Pat</code> does (expression context and the <code>if … is …</code> form). It's the predicate spelling of \"does this not match\"; for the <code>if</code> form, the body runs when the pattern does not match.</p> <p>Or peel off the <code>Some</code> with <code>let .Some(v) = y else { ... }</code>:</p> <pre><code>let .Some(v) = y else { return -1 }\n// v is in scope and has type int64\n</code></pre>","path":["Language guide","Types"],"tags":[]},{"location":"types/#postfix","level":3,"title":"Postfix <code>?</code>","text":"<p><code>expr?</code> on a <code>T?</code> value evaluates to <code>T</code>. Without a trailing clause it panics on <code>none</code>; with <code>or default</code> it substitutes the default instead:</p> <pre><code>let n: int64? = find(\"hello\", 'l')\nlet idx: int64       = n?           // panics if none\nlet idx2: int64      = n? or -1     // -1 if none\n</code></pre> <p>The default is evaluated lazily (only when the value is <code>none</code>), and its type must match the unwrapped <code>T</code>. A following <code>or</code> outside the unwrap stays a boolean operator: <code>x? or 1</code> substitutes 1 for none, but <code>(x?) or some_bool</code> is a boolean-or on the unwrapped value (and a type error unless <code>T</code> is <code>bool</code>).</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#arrays","level":2,"title":"Arrays","text":"<p><code>[T]</code> is a resizable array of element type <code>T</code>:</p> <pre><code>let nums: [int64] = [1, 2, 3]\nlet names: [str] = [\"alice\", \"bob\"]\n</code></pre> <p>See Arrays for indexing, slicing, and concatenation.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#structs","level":2,"title":"Structs","text":"<p>Named product types:</p> <pre><code>type Point = struct {\n    x: int64\n    y: int64\n}\n</code></pre> <p>See Structs.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#enums","level":2,"title":"Enums","text":"<p>Tagged unions:</p> <pre><code>type Shape = enum {\n    Circle(radius: int64)\n    Rect(w: int64, h: int64)\n}\n</code></pre> <p>See Enums.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#generics","level":2,"title":"Generics","text":"<p>Type-parametric structs are monomorphised at compile time:</p> <pre><code>type Pair[A, B] = struct {\n    first: A\n    second: B\n}\n\nlet p: Pair[str, int64] = Pair[str, int64](first: \"hello\", second: 42)\n</code></pre> <p><code>Map[K, V]</code> (in <code>builtins</code>) is the canonical example.</p>","path":["Language guide","Types"],"tags":[]},{"location":"types/#interfaces","level":2,"title":"Interfaces","text":"<p>Method-based contracts that types can opt into by declaring the right methods. Primitives implement them implicitly via boxing.</p> <pre><code>interface Stringable {\n    func to_str(self) -&gt; str\n}\n\nfunc show(anon x: Stringable) -&gt; none {\n    print(x.to_str())\n}\n\nshow(42)        // works — int64 implements Stringable via primitives/int64.kod\nshow(true)      // same for bool\n</code></pre>","path":["Language guide","Types"],"tags":[]},{"location":"types/#fallible-types-t-or-error","level":2,"title":"Fallible types: <code>T or Error</code>","text":"<p>A function declared <code>-&gt; T or Error</code> returns a <code>Result</code> cell — either an <code>Ok(T)</code> or an <code>Err(Error)</code>. Use <code>try</code> to propagate, <code>must</code> to panic, or pattern-match the result directly.</p> <pre><code>type IoError = struct {\n    message: str\n    func to_str(self) -&gt; str { return self.message }\n}\n\nfunc read_size(anon path: str) -&gt; int64 or Error {\n    throw IoError(message: f\"file not found: {path}\")\n}\n\nfunc main() -&gt; int64 {\n    let n: int64 = must read_size(\"missing.txt\")  // panics on Err\n    return n\n}\n</code></pre> <p>To inspect a result instead of propagating or panicking, pattern-match it with <code>Ok</code> / <code>Err</code> (the two-armed analog of <code>Some</code> / <code>none</code>). <code>Ok</code>'s binding has the result's value type; <code>Err</code>'s is the <code>Error</code> interface:</p> <pre><code>match read_size(\"missing.txt\") {\n    Ok(n)  -&gt; print_int(n)\n    Err(e) -&gt; print(e.to_str())\n}\n</code></pre> <p>The same patterns work with <code>if … is</code> and the bare <code>is</code> operator, which is handy in tests:</p> <pre><code>let r = read_size(\"missing.txt\")\nassert r is Err(_)              // it failed\nif r is Err(e) { print(e.to_str()) }\n</code></pre>","path":["Language guide","Types"],"tags":[]},{"location":"types/#recovering-the-concrete-error-type","level":3,"title":"Recovering the concrete error type","text":"<p><code>Err(e)</code> binds <code>e</code> as the <code>Error</code> interface. To get back the concrete error struct, downcast with <code>is StructType</code> / <code>is StructType(binding)</code> — a general interface downcast that tests the boxed type and, on a match, binds the typed value:</p> <pre><code>match read_size(path) {\n    Ok(n)  -&gt; use(n)\n    Err(e) -&gt;\n        if e is NotFound(nf) {\n            print(f\"missing: {nf.path}\")\n        } else {\n            print(e.to_str())\n        }\n}\n</code></pre> <p>It works in <code>match</code>, <code>if … is</code>, and the bare <code>is</code> operator (a <code>match</code> on an interface needs an <code>else</code>, since its set of implementors isn't enumerable).</p>","path":["Language guide","Types"],"tags":[]}]}