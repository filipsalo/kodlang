func add_one(anon n: int64) -> int64 {
    return n + 1
}

test "transitive_dep: add_one zero" {
    assert add_one(0) == 1
}

test "transitive_dep: add_one negative" {
    assert add_one(-5) == -4
}

// expected mode:
// skip
