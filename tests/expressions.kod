// Basic expressions
func main() -> int64 {
	print("ok")
	let xit: int64 = 1
	// Can't print integers yet, so we'll use the exit code
	return xit + 1
}

// expected output:
// ok

// expected status:
// 2
