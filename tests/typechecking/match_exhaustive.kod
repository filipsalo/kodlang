type Color = enum {
	Red
	Green
	Blue
}

func main() -> int64 {
	let c: Color = .Red
	match c {
		.Red -> print("red")
		.Green -> print("green")
	}
	return 0
}

// expected errors:
// 9:8: error: match on `Color` doesn't cover variant `Blue`
