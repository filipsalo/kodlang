test "addition works" {
    let n: int64 = 2 + 2
    if n != 4 {
        fail(f"expected 4, got {n}")
    }
}

test "this one fails on purpose" {
    fail("this is what failure looks like")
}

test "string equality" {
    let s: str = "hello" + " " + "world"
    if s != "hello world" {
        fail(f"got {s}")
    }
}

// expected mode:
// skip
