// A `let` binding is immutable; reassigning it is a compile error.
// (Declaring `mut limit` instead would make this compile.)
func main() -> int64 {
    let limit: int64 = 10
    limit = 20
    return limit
}

// expected errors:
// 5:5: cannot reassign immutable binding `limit`; declare it with `mut`
