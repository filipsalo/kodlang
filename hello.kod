extern func strlen(s: str) -> i64
extern func write(fd: i32, buf: str, len: i64) -> i64

func print(string: str) -> None {
	write(1, string, strlen(string))
}

func main() -> int32 {
	// This is a comment
	print("Hello, world!")
}

