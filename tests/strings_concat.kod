func main() -> int64 {
	let a: str = "hello"
	let b: str = " world"
	let c: str = a + b
	print(c)
	a += "!"
	print(a)
	return 0
}

// expected output:
// hello world
// hello!

// expected status:
// 0

// expected selfhosted:
// skip
