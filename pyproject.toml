[project]
name = "kod"
version = "0.1.0"
dependencies = [
  "mypy",
  "pytest",
]
requires-python = ">=3.12"

[project.scripts]
kod = "kod:__main__"

[tool.setuptools]
packages = [
  "kod",
]

[tool.ruff.lint]
extend-select = [
  "I",  # isort
]
