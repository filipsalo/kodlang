import "./transitive_dep"

test "entry: importing module sees the imported function" {
    assert transitive_dep.add_one(41) == 42
}

test "entry: another local test" {
    assert "hi" + " there" == "hi there"
}

// expected mode:
// skip
